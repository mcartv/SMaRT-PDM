DO $$ BEGIN BEGIN ALTER TABLE public.messages ADD COLUMN room_id UUID NULL REFERENCES public.chat_rooms(room_id) ON DELETE CASCADE; EXCEPTION WHEN duplicate_column THEN RAISE NOTICE 'column room_id already exists in messages table.'; END; END $$; DO $$ BEGIN ALTER TABLE public.messages ALTER COLUMN receiver_id DROP NOT NULL; EXCEPTION WHEN OTHERS THEN RAISE NOTICE 'receiver_id is already nullable or an error occurred.'; END $$;
