services:
  - type: web
    name: smart-pdm-backend
    env: node
    rootDir: admin/backend
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    autoDeploy: true
